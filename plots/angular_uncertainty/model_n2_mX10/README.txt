THESE FILES: 

generated for mX = 10 MeV, with n=2 form factor.

Using the version of "UP" and "FACE" vectors from the normal vector data (not orthogonal). 

Assuming that (a*, b*, c*) vectors are unit vectors.

R(t) includes contributions from s1, s2, s5, s8.  (No s2 or s3!)